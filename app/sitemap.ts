import type { MetadataRoute } from "next";import {jobs}from"./data";export default function sitemap():MetadataRoute.Sitemap{const base="https://jdemnato.cz",now=new Date();const pages=["","/prace","/zakazky","/dodavatele","/dodavatele/seznam","/registrace","/pro-uchazece","/pro-dodavatele","/pro-zamestnavatele","/o-nas","/kontakt","/podminky","/ochrana-osobnich-udaju","/cookies"];return[...pages.map((path,index)=>({url:`${base}${path}`,lastModified:now,changeFrequency:(index<5?"daily":"monthly") as "daily"|"monthly",priority:index===0?1:index<5?.9:.5})),...jobs.filter(j=>new Date(j.expires)>=now).map(j=>({url:`${base}/prace/${j.slug}`,lastModified:new Date("2026-08-18"),changeFrequency:"weekly" as const,priority:.8}))]}

